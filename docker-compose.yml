services:
  devin-automation:
    build: .
    ports:
      - "5050:5050"
    env_file:
      - .env
    volumes:
      - sessions-data:/app/data
    restart: unless-stopped

volumes:
  sessions-data:
